# Heretek Tier 1 — Android Gradle configuration
# The container uses AndroidX (appcompat/material/webkit), so AndroidX must be enabled.
android.useAndroidX=true
android.nonTransitiveRClass=true
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
